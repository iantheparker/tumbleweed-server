** pre-requisites **
# install rvm, instructions at ->  https://rvm.io/

rvm install 1.9.3
rvm use 1.9.3 # also add this to the end of your ~/.bash_profile so all terminal windows will use this version of ruby by default
gem install heroku

https://devcenter.heroku.com/articles/quickstart
https://devcenter.heroku.com/articles/rails3
https://devcenter.heroku.com/articles/multiple-environments

send a post to tumbleweed.me/register
with fid and devToken

run rails locally hit
rails server  -within the tweed directory

for live on heroku-
git push heroku master
*make sure heroku has access to keys - heroku keys:add ~/.ssh/id_rsa.pub

do this before raking the stuff in
rails g migration nameofmigration

for any changes to data types, tables, whatev for local
rake db:migrate

on heroku hit
heroku run rake db:migrate

let's me watch the logs
heroku logs --tail
heroku logs --tail --remote production


to clear a table
rails c
User.delete_all
User.destroy_all
* i may not be blowing away the table properly because i didn't get a register apn
* make sure i'm crystal clear on keeping staging and prod up to date and using/not same db

*test fourquare/push url
?"foursquare_id"=1234&device_token=1234&first_name=testy&last_name=testerson

curl -v -H "Content-Type: application/json" -X POST -d '{"screencast":{"subject":"tools"}}' \
http://localhost:3000/raw_checkins.json

curl -v -H "Content-Type: application/json" -X POST -d '{"foursquare_id": "1234",
    "device_token": "1234",
    "first_name": "testy",
    "last_name": "testerson"}' \
http://localhost:3000/register.json