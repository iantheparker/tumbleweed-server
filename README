** pre-requisites **
# install rvm, instructions at ->  https://rvm.io/

rvm install 1.9.3
rvm use 1.9.3 # also add this to the end of your ~/.bash_profile so all terminal windows will use this version of ruby by default
gem install heroku


send a post to tumbleweed.me/register
with fid and devToken

run rails locally hit
rails server  -within the tweed directory

for live on heroku-
git push heroku master

do this before raking the stuff in
rails g migration nameofmigration

for any changes to data types, tables, whatev for local
rake db:migrate

on heroku hit
heroku run rake db:migrate

let's me watch the logs
heroku logs --tail

